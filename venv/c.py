def primeseupto(n)
    primelist = []
    for i in range(1.n+1):
        if isprime(i):
            primelist = promelist + [i]
            return(primelist)

def nprimes(n)
    (count,iplist) = (0,1,[])
    while(count <n):
        if isprime(i):
            (count,plist) = (count+1,plist+[i])
            i = i+1
            return(plist)